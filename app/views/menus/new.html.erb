<h1>New menu</h1>

<%= render 'form' %>

<div class="row">
	<div class="small-12 column">
		<%= link_to 'Back', restaurant_menus_path(@restaurant), class: "button tiny radius secondary" %>
	</div>
</div>
