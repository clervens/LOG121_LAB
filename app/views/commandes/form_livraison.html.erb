<%= form_for [@restaurant, @livraison] do |f| %>
  <%= f.hidden_field :commande_id %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :adresse %>
  <%= f.submit nil, class: "right button small radius success" %>
<% end %>