<h1>Listing commandes</h1>

<table class="responsive">
  <thead>
    <tr>
      <th>#</th>
      <th>Restaurant</th>
      <th><%= Commande.human_attribute_name "numero" %></th>
      <th><%= Commande.human_attribute_name "date_de_livraison" %></th>
      <th><%= Commande.human_attribute_name "created_at" %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @commandes.each do |commande| %>
      <tr>
        <td data-th="#"><%= commande.id %></td>
        <td data-th="Restaurant"><%= link_to commande.restaurant.nom, commande.restaurant %></td>
        <td data-th="<%= Commande.human_attribute_name "numero" %>"><%= commande.numero %></td>
        <td data-th="<%= Commande.human_attribute_name "date_de_livraison" %>"><%= l commande.date_de_livraison, format: :short %></td>
        <td data-th="<%= Commande.human_attribute_name "created_at" %>"><%= l commande.created_at, format: :short %></td>
        <td>
          <ul class="button-group radius">
            <li><%= link_to 'Show', commande, class: "button tiny secondary" %></li>
            <li><%= link_to 'Edit', edit_commande_path(commande), class: "button tiny secondary" %></li>
            <li><%= link_to 'X', commande, method: :delete, data: { confirm: 'Are you sure?' }, class: "button tiny alert" %></li>
          </ul>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<style>li a.button{margin:0;}</style>
<%= link_to 'New Commande', new_commande_path, class: "button tiny radius secondary" %>